{
  "spec": "sessions.fyi/release-manifest/v1",
  "status": "production",
  "status_note": "Live and battle-tested: this exact image runs in a production AWS Nitro enclave guarding real Telegram accounts. Not a development, staging, or testnet build. The release is commit-pinned and reproducible; trust comes from rebuilding it and matching the PCR0 below, plus the live in-browser attestation.",
  "release_tag": "v1.0.1",
  "pcr0_g": "bdb710f87b683a235e27a2a7413c83769d89183d1730ea69dfd2d9c5dc56548f329a6b6c1064861405a0d6d8d50fceef",
  "gateway_commit": "e4813dcafb16a8232d8631e10cd8d3f8f6c94946",
  "gateway_commit_repo": "github.com/Danimal01/enclave-gateway (THIS repository) — resolve gateway_commit here, not elsewhere; it is the commit whose source builds to pcr0_g.",
  "allowlist_file": "tg-chokepoint.mjs",
  "allowlist_sha256": "733d216491e4e593dc07bd34aee4c65385a0ae614301d474e61bf98ef1f5539f",
  "allowlist_sha256_preimage": "IMPORTANT: allowlist_sha256 is INTENTIONALLY DIFFERENT from files[].sha256 below and is NOT supposed to equal it (this is not a mismatch). allowlist_sha256 = sha256 over the UTF-8 string '// FILE tg-chokepoint.mjs\\n' concatenated with the file's bytes -- the attestation-binding digest the live attestation binds and the how-it-works page verifies. files[].sha256 = the plain `sha256sum tg-chokepoint.mjs`. Two verifiers, two values, same file.",
  "files": [
    { "name": "tg-chokepoint.mjs", "sha256": "523899749491033fe20d17dbfb80cf600f2f72932fb7653c0b4da08df3084412", "note": "raw sha256 of the file bytes; matches `sha256sum tg-chokepoint.mjs`. Intentionally DIFFERS from the top-level allowlist_sha256 (the attestation-binding digest) -- see allowlist_sha256_preimage." }
  ],
  "binaries": [
    { "name": "kmstool_enclave_cli", "sha256": "754d5b16d458ad9925578543db78bde92df4bc7306b84c8983961ab7c9d33e8e", "source": "aws-nitro-enclaves-sdk-c v0.4.5 (commit cd61b61) with patches/apply-kmstool-context-patch.py applied; built via containers/Dockerfile.al2 --target kmstool-enclave-cli" },
    { "name": "libnsm.so", "sha256": "b020f96e39162024bb5248408ce12d9997b049549782f0d65511dfbd3782b0e6", "source": "same aws-nitro-enclaves-sdk-c v0.4.5 build" },
    { "name": "gvforwarder", "sha256": "0f85431ef3dbec8a4fa5bd1ed05dc418a4938e112e4879496056ab23163ecc7f", "source": "gvisor-tap-vsock release, arm64 (aarch64)" }
  ],
  "reproducible": true,
  "reproducible_note": "umask-independent: a 644 and a 664 source context build the IDENTICAL PCR0 (the Dockerfile canonicalizes every COPYed file's mode; .gitattributes pins LF). The three host-staged binaries above are pinned by sha256 so a verifier can confirm them. Build on any host and match.",
  "transparency": "To verify: check out tag v1.0.1 of THIS repository (or gateway_commit above), rebuild per BUILD.md, and confirm the PCR0 matches pcr0_g. gateway_commit resolves in this repo only; doc-only commits may sit after it on the tag, but the source that builds to pcr0_g is exactly that commit.",
  "attestation": {
    "live_artifact": "https://sessions.fyi/attestation/gateway.json",
    "verifier": "https://sessions.fyi/how-it-works",
    "note": "A live AWS Nitro enclave running this exact image attests PCR0_G above (the live artifact is authoritative for the current module id and timestamp). The capability allowlist (tg-chokepoint.mjs) is account-management only: no message, chat, file, or contact method."
  },
  "note": "Production release v1.0.1: INSTANT new-login detection. The minimal MTProto client unwraps the standard update containers and subscribes (updates.getState) on connect, so a new-login push is acted on the instant it arrives; the fallback sweep is tightened to a ~45s base (jittered, within Telegram read limits). Builds on the v1.0.0 per-account self-heal. The allowlist CAPABILITY SET is unchanged from v1.0.0 (no method added/removed); allowlist_sha256 changed only because a comment referencing internal tests was removed from tg-chokepoint.mjs. Deterministic, umask-independent reproducibility."
}
